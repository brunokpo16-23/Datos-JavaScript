// Ojeto MATH
let resultado;

resultado = Math.PI; // Este nos dara el resultado de PI (3,14)
resultado = Math.random; // Nos dara un numero al azar 
resultado = Math.round(2.5); // Nos redondeara el numero 
resultado = Math.ceil(2.1); // Siempre nos redondeara hacia arriba 
resultado = Math.floor(2.1); // Siempre nos redondeara hacia abajo
resultado = Math.sqrt(144); // Este nos da la raiz cuadrada de nuestro numero 
resultado = Math.abs(-200); // Este convierte un numero negativo en positivo 
resultado = Math.min(3, 5, 8, 1, 7); // Este nos indica cual es el numero menor de la serie de numeros 
resultado = Math.max(3, 5, 8, 1, 7); // Este nos indica cual es el numero mayor de la serie de numeros 


console.log(resultado);